@foreach (var v in Value)
{
    <TreeItem Value="v" Expandable="RenderChildren" Text="@v.ToString()">
            
    </TreeItem>
}

@code {
    [Parameter] public ICollection<MethodDefinition> Value { get; set; }
    [Parameter] public bool RenderChildren { get; set; } = true;
}